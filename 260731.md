# 2026-07-31 작업 기록

> 이 문서는 오늘(2026-07-31) 진행한 작업 전체를 코드 변경 전/후 비교와 함께
> 상세히 기록한다. 전날(2026-07-30, `260729.md`의 다음 세션)은 학습된
> 평가함수(`evaluate_learned`) 파이프라인 구축·정규화 스윕·`ISMCTSAI` 연결·
> `rollout_turn_cap` 튜닝·웹 난이도 통합까지 진행했고, 오늘은 그 위에서
> 실제로 웹으로 플레이해보며 발견한 버그 수정과 UI/UX 개선에 집중했다.
> 전날 작업의 최종 산출물은 `ai_train_pipeline.md`, `ai_features_reference.md`,
> `ai_regularization.md`, `ai_howtodiversity.md`와 `src/game/ai_ismcts_learned.py`
> (`ISMCTSLearnedAI` 프리셋)에 정리돼 있다.

## 0. 오늘 진행한 작업 개요

| # | 작업 | 파일 | 유형 |
|---|---|---|---|
| 1 | `Smoke_0`/`Life_0` AI 채점 버그 수정 | `src/game/ai_prior.py`, `tests/test_ai_prior.py` | 버그 수정 |
| 2 | 웹 AI 난이도 선택 버튼 한 줄 정렬 | `web/templates/index.html` | UI 수정 |
| 3 | 다크/파스텔 색상 테마 토글 추가 | `web/app.py`, `web/templates/index.html`, `web/static/css/style.css`, `web/static/js/app.js` | 신규 기능 |
| 4 | 테마 토글 버튼 위치 조정 (우측하단) | `web/static/css/style.css` | UI 수정 |
| 5 | AI 턴 진행 속도(페이싱) 조정 | `web/static/js/app.js` | UX 개선 |
| 6 | "상대 손패" 표시가 AI 턴 중 뒤바뀌는 버그 수정 | `web/static/js/app.js` | 버그 수정 |
| 7 | 학습된 eval 기준 하이퍼파라미터 재점검 (4단계 스윕) | `scripts/sweep_ismcts_learned.py` (신규) | 검증/튜닝 |
| 8 | 학습된 eval 자기개선 루프 1세대 시도 (결과: 무의미, 미채택) | `scripts/generate_selfplay_data.py` | 실험 |
| 9 | Main3/Aux3 포팅 — 프로토콜 15개·카드 90장 (180→270장) | `engine.py`, `protocols.py`, `carddefs.py`, `card_text.py`, `ai_prior.py`, 웹 도감/밴픽 UI | 신규 기능 |
| 10 | 실전 플레이로 발견한 버그 4건 수정 (손패 노출/AI 라인선택/afterRearrange 미배선/표기) | `web/app.py`, `ai_prior.py`, `engine.py`, `carddefs.py` | 버그 수정 |
| 11 | Main3/Aux3 90장 전수 검증 (루아 대조 + reactive 배선 감사 + 대규모 자기대국) | `carddefs.py`, `tests/test_carddefs_main3_mechanics.py` | 검증/버그 수정 |

1~6은 전부 **기존 파일 수정**, 7은 신규 파일(`scripts/sweep_ismcts_learned.py`)
추가, 8은 기존 파일 리팩터링(`RecordingAI` 위임 구조화) + 데이터/가중치
아카이브 파일 추가다. 순서대로 상세히 기록한다.

---

## 1. `Smoke_0`/`Life_0` AI 채점 버그 수정

### 1.1 문제 제기

사용자가 "연기 0 카드가 버그가 있는거 같아"라고 보고했다 — 구체적인
재현 방법 없이 "확인해봐"만 요청받은 상태였다.

### 1.2 조사 과정

`연기 0` = `Smoke_0`. 먼저 `src/game/carddefs.py`의 카드 효과 구현
자체(`_smoke_0_play`)를 확인했다:

```python
def _smoke_0_play(g, c):
    # "뒷면 카드가 있는 각 라인에서" -- Smoke_0 자신의 라인도 뒷면 카드를
    # 가지면 자격이 있다. 생명0과 동일하게, 자기 자신이 가려지면 즉시
    # 중단된다(공식 FAQ) -- 자기 라인 처리 순서가 전략적 선택이 됨.
    eligible = [line for line in (1, 2, 3) if g.facedown_in_line(line) > 0]
    _play_top_face_down_each(g, c, eligible)
```

카드 텍스트(`card_text.py`: "뒷면 카드가 있는 각 라인에 자신의 덱 맨
위 카드를 뒷면으로 플레이합니다.")와 정확히 일치했고, 기존
`tests/test_carddefs_smoke.py`의 7개 테스트도 전부 통과했다 — **카드
효과 자체는 정상**이었다.

다음으로 AI가 이 카드를 어떻게 채점하는지(`src/game/ai_prior.py`)를
확인했다:

```python
# 수정 전
TAGS["Smoke_0"] = {"deck_plays": 2}
```

```python
# 수정 전, effect_prior() 안
if tag.get("deck_plays"):
    # 손패를 안 쓰고 덱에서 뒷면으로 내는 공짜 판 진전 (Water_1/Life_0 등).
    # 뒷면 카드는 항상 값 2로 취급되니 그 정도 기여로 어림.
    s += 1.0 * tag["deck_plays"]
```

**여기서 문제를 발견했다**: `Smoke_0`의 실제 효과는 "뒷면 카드가 있는
라인마다"(0~3장, 조건부)인데, AI 채점은 **항상 고정 2.0점**을 준다.
직접 재현해봤다:

```python
from src.game.engine import Engine
from src.game.ai_prior import effect_prior

e = Engine(protocols1=['Smoke','Water','Fire'], protocols2=['Ice','Metal','Death'])
c = e.new_card('Smoke', 0, 1)
c.face_up = True
print(effect_prior(e, 1, c))   # 결과: 2.0 -- 보드에 뒷면 카드가 하나도 없는데도!
```

보드에 뒷면 카드가 하나도 없어서 `Smoke_0`를 내도 **실제로는 아무
일도 일어나지 않는** 상황에서도 AI는 2.0점을 매기고 있었다 — 즉 AI가
이 카드를 상황과 무관하게 항상 과대평가하는 구조적 버그였다.

같은 헬퍼 함수(`_play_top_face_down_each`)를 공유하는 `Life_0`도
확인해보니 같은 종류의 문제였다("내 카드가 있는 라인마다"인데
`TAGS["Life_0"] = {"deck_plays": 2, "ongoing": True}`로 역시 고정값).
다만 조건이 "내 카드가 있는 라인"이라 게임이 조금만 진행돼도 대체로
참이 되기 쉬워서 증상은 `Smoke_0`보다 약했다.

### 1.3 수정 내용

`ai_prior.py`엔 이미 `del`/`ret`/`flip` 효과가 **판 상황을 직접 읽어서**
값을 매기는 패턴(`_del_prior`, `_ret_prior`, `_flip_prior`)이 있었다 —
같은 방식으로 `deck_plays`도 조건부일 때 실제 자격 라인 수를 세도록
바꿨다.

**신설 함수** (`_flip_prior` 바로 뒤, `effect_prior` 앞에 추가):

```python
def _deck_plays_prior(g, pi, spec):
    """deck_plays 효과 1건의 기대 가치. spec이 그냥 숫자면 무조건 n장을
    뒷면으로 내는 카드(예: Water_1 -- 항상 정확히 2라인, 조건 없음).
    spec이 dict면 "그 조건을 만족하는 라인마다 1장씩"인 카드(Smoke_0/Life_0)
    -- 고정 개수가 아니라 지금 판에서 실제로 몇 라인이 자격 있는지 세어야
    정확하다(자격 라인이 0개면 실제로도 아무 일도 안 일어남)."""
    if not isinstance(spec, dict):
        return 1.0 * spec
    pred = spec.get("eligible")
    if pred == "facedown_line":  # Smoke_0: 뒷면 카드가 있는(양쪽 다 후보) 라인마다
        n = sum(1 for l in (1, 2, 3) if g.facedown_in_line(l) > 0)
    elif pred == "own_line":  # Life_0: 내 카드가 있는 라인마다
        n = sum(1 for l in (1, 2, 3) if g.players[pi]["stacks"][l])
    else:
        n = spec.get("n", 0)
    return 1.0 * n
```

**`effect_prior()` 호출부 변경**:

```python
# 수정 후
if tag.get("deck_plays"):
    # 손패를 안 쓰고 덱에서 뒷면으로 내는 공짜 판 진전 (Water_1/Smoke_0/
    # Life_0 등). 뒷면 카드는 항상 값 2로 취급되니 그 정도 기여로 어림 --
    # 조건부 카드(Smoke_0/Life_0)는 _deck_plays_prior가 지금 판에서
    # 실제 자격 라인 수를 센다(고정값이 아님).
    s += _deck_plays_prior(g, pi, tag["deck_plays"])
```

**TAGS 변경**:

```python
# 수정 후
TAGS["Smoke_0"] = {"deck_plays": {"eligible": "facedown_line"}}  # 뒷면 카드 있는 라인마다 -- 고정 2가 아님
...
TAGS["Life_0"] = {"deck_plays": {"eligible": "own_line"}, "ongoing": True}  # 내 카드 있는 라인마다 -- 고정 2가 아님
```

`Water_1`처럼 진짜 무조건부인 카드(`{"deck_plays": 2}`, 항상 정확히
2라인)는 `_deck_plays_prior`가 `isinstance(spec, dict)` 분기에서 그냥
`1.0 * spec`을 반환하므로 **기존 동작이 그대로 보존**된다 — 이번 수정은
조건부 카드 2장(`Smoke_0`, `Life_0`)에만 영향을 준다.

### 1.4 검증

수정 후 재현 스크립트로 확인:

```
뒷면 카드 0개: 0.0   (수정 전: 2.0)
뒷면 카드 1개(라인1): 1.0
뒷면 카드 2개(라인1,2): 2.0
```

`tests/test_ai_prior.py`에 회귀 테스트 3개 추가:

```python
def test_smoke_0_effect_prior_scales_with_eligible_facedown_lines():
    e = Engine(protocols1=["Smoke", "Water", "Fire"], protocols2=["Ice", "Metal", "Death"])
    c = _card(e, "Smoke", 0, 1)
    assert effect_prior(e, 1, c) == 0.0          # 자격 라인 0개
    fd1 = _card(e, "Water", 3, 2, face_up=False)
    e.players[2]["stacks"][1].append(fd1)
    assert effect_prior(e, 1, c) == pytest.approx(1.0)   # 자격 라인 1개
    fd2 = _card(e, "Ice", 1, 1, face_up=False)
    e.players[1]["stacks"][2].append(fd2)
    assert effect_prior(e, 1, c) == pytest.approx(2.0)   # 자격 라인 2개


def test_life_0_effect_prior_scales_with_own_nonempty_lines():
    """Life_0은 ongoing 태그도 있어 deck_plays가 0이어도 기본 0.8점은
    남는다 -- deck_plays 쪽만 실제 라인 수만큼 늘어나는지 그 증분으로 확인."""
    e = Engine(protocols1=["Life", "Water", "Fire"], protocols2=["Ice", "Metal", "Death"])
    c = _card(e, "Life", 0, 1)
    base = effect_prior(e, 1, c)
    assert base == pytest.approx(0.8)
    mine = _card(e, "Water", 3, 1, face_up=True)
    e.players[1]["stacks"][1].append(mine)
    assert effect_prior(e, 1, c) == pytest.approx(base + 1.0)


def test_water_1_unconditional_deck_plays_still_flat_two():
    """무조건부 deck_plays(정수 그대로)는 기존처럼 보드 상태와 무관하게
    고정값이어야 한다 -- 이번 수정이 조건부 카드 케이스만 건드렸는지 확인."""
    e = Engine(protocols1=["Water", "Fire", "Life"], protocols2=["Ice", "Metal", "Death"])
    c = _card(e, "Water", 1, 1)
    assert effect_prior(e, 1, c) == pytest.approx(2.0)
```

전체 테스트 스위트 390개 통과(당시 기준).

---

## 2. 웹 AI 난이도 선택 버튼 한 줄 정렬

사용자가 게임 설정 화면 스크린샷을 첨부하며 "중급" 버튼이 줄바꿈돼
다음 줄에 혼자 떨어지는 게 보기 안 좋다고 지적했다.

### 원인

`web/static/css/style.css`에 이미 3열용 CSS 클래스(`.mode-select.three`,
`grid-template-columns: 1fr 1fr 1fr`)가 있었는데, AI 난이도 선택
`<div class="mode-select">`(버튼 3개: 왕초보/초보/중급)엔 이 클래스가
안 붙어 있어서 기본값인 2열 그리드(`.mode-select`, `1fr 1fr`)를 쓰고
있었다 — 그래서 3번째 버튼이 다음 줄로 밀려났다.

### 수정

`web/templates/index.html`:

```html
<!-- 수정 전 -->
<p class="setup-step-label">AI 난이도는요?</p>
<div class="mode-select">

<!-- 수정 후 -->
<p class="setup-step-label">AI 난이도는요?</p>
<div class="mode-select three">
```

클래스 하나만 추가하는 최소 변경. Playwright로 브라우저에서 재확인해
세 버튼이 한 줄에 나란히 배치되는 걸 확인했다.

---

## 3. 웹 UI: 다크/파스텔 색상 테마 토글 추가

### 3.1 요청

"브라우저에서 웹에 접속하면 지금 전체적인 색깔이 너무 사이버펑키하고...
약간 밝은 톤(파스텔톤)의 색깔테마로도 고를 수 있게... 지금 프로토콜에
할당된 색의 느낌은 유지하면서."

### 3.2 기존 아키텍처 조사

`web/static/css/style.css`가 색을 CSS 커스텀 프로퍼티(변수) 하나로
모아둔 구조였다:

```css
:root {
  --bg-void: #06070a;
  --bg-panel: #0d1016;
  --bg-panel-raised: #12161f;
  --bg-card-back: #171b26;
  --line-hair: rgba(255, 255, 255, 0.08);
  --phosphor: #3dffa0;
  --phosphor-dim: #1f8f5c;
  --phosphor-glow: rgba(61, 255, 160, 0.35);
  --amber: #ffb454;
  --danger: #ff5c72;
  --text-hi: #eef2f0;
  --text-mid: #9fb0ab;
  --text-dim: #5c6b67;
}
```

거의 모든 색 규칙이 이 변수들을 참조하고 있어서, **변수 값만 바꾸면
전체 UI 색이 자동으로 바뀌는** 구조였다 — 테마 토글 구현에 유리한
조건이었다.

프로토콜별 고유색은 `src/game/protocols.py`의 `COLOR` 딕셔너리(RGB
0~1 float, 30개 프로토콜)가 원본이었고, `web/app.py`가 이걸
`rgb(...)` CSS 문자열로 변환해 `index.html`의 `<script id="proto-data">`
JSON 블록으로 클라이언트에 내려주고 있었다. JS(`app.js`)는
`PROTO.colors[protoId]`로 읽어서 카드 등에
`style.setProperty("--card-accent", ...)`로 인라인 적용하는 구조였다
(15곳 안팎에서 사용).

### 3.3 설계 결정 — 프로토콜 색 변환

원본 색(`protocols.py`의 `COLOR`)은 **검은 배경에서 네온으로 빛나게**
튜닝된 값이라, 그대로 밝은 배경의 테두리/글자색으로 쓰면 문제가
생긴다:
- 매우 밝은 색(`Light`, `Peace`, `Clarity` 등)은 흰 배경과 거의
  구분이 안 됨
- 이미 어두운 색(`Hate`, `Death` 등)은 그럭저럭 괜찮음
- → 프로토콜마다 밝기가 들쭉날쭉해서 그대로는 "고른 파스텔 팔레트"가
  안 나옴

`colorsys`(파이썬 표준 라이브러리)로 HSL 변환 후, **색상(H)은 그대로
유지**하고 **채도(S)/명도(L)만 좁은 목표 띠로 다시 맞추는** 방식을
선택했다:

```python
def _pastel_variant(rgb):
    """protocols.py의 원색(검은 배경용 네온)을 밝은/파스텔 테마에서도 쓸 수
    있게 변환한다. 색상(H)은 그대로 둬서 "이 프로토콜은 이 색"이라는 정체성은
    유지한다.

    채도(S)는 원래 값에 비례시키지 않고 좁은 목표 띠(0.30~0.50)로 다시
    맞춘다 -- 원래 채도를 그대로 비례 축소하면 역효과가 난다: Clarity/Peace
    처럼 "아주 밝아서 눈에는 옅어 보이지만 HSL 채도 자체는 이미 높은" 색을
    그대로 어둡게(밝은 배경에서 테두리/글자로 쓸 수 있게)만 만들면, 밝기만
    빠지고 채도는 유지돼 오히려 쨍한 색이 튀어나온다. 채도를 아예 좁은
    파스텔 띠로 다시 맞춰야 "같은 계열의 파스텔"처럼 고르게 느껴진다.

    명도(L)도 밝은 배경 위 테두리/글자로 쓸 수 있는 중간 밝기 띠
    (0.36~0.56)로 누르되, (l - 0.5) 항으로 원래 밝기 순서를 약하게나마
    보존한다(예: 원래 더 밝았던 프로토콜이 파스텔 버전에서도 상대적으로
    더 밝게 남는다)."""
    r, g, b = rgb
    h, l, s = colorsys.rgb_to_hls(r, g, b)
    s2 = 0.30 + 0.20 * min(s, 1.0)
    l2 = max(0.36, min(0.56, 0.44 + 0.08 * (l - 0.5)))
    return colorsys.hls_to_rgb(h, l2, s2)
```

이 함수는 처음에 채도를 원래 값에 **비례**시키는 버전(`s2 = min(s *
0.72, 0.62)`)으로 시도했다가, `Clarity`(옅은 분홍)가 오히려 쨍한
마젠타로 변하는 부작용을 실측으로 발견하고(HSL에서 "옅어 보이는"
색도 채도 자체는 높을 수 있어서, 밝기만 낮추면 역설적으로 색이
진해짐) 지금의 "좁은 목표 띠로 재설정" 버전으로 교체한 것이다. 예시
검증(파이썬 REPL):

```
Water   -> rgb(38,165,255)  -> rgb(56,123,170)   (하늘색 -> 차분한 데님블루)
Fire    -> rgb(255,132,20)  -> rgb(169,110,56)   (주황 -> 테라코타)
Clarity -> rgb(255,204,232) -> rgb(180,60,126)   (옅은 분홍 -> 더스티로즈, 과도한 마젠타 방지)
Hate    -> rgb(229,33,38)   -> rgb(164,60,63)    (빨강 -> 브릭레드)
```

### 3.4 구현

**`web/app.py`**: 다크/라이트 두 팔레트를 서버에서 미리 계산해 함께
내려준다.

```python
# 수정 전
proto_colors = {
    p: f"rgb({int(r*255)},{int(g*255)},{int(b*255)})"
    for p, (r, g, b) in Protocols.COLOR.items()
}
...
return render_template("index.html", proto_colors=proto_colors, ...)
```

```python
# 수정 후
def _pastel_variant(rgb):
    ...  # 위 §3.3

def _rgb_css(rgb):
    r, g, b = rgb
    return f"rgb({int(r*255)},{int(g*255)},{int(b*255)})"

@app.route("/")
def index():
    proto_colors_dark = {p: _rgb_css(rgb) for p, rgb in Protocols.COLOR.items()}
    proto_colors_light = {p: _rgb_css(_pastel_variant(rgb)) for p, rgb in Protocols.COLOR.items()}
    ...
    return render_template("index.html", proto_colors_dark=proto_colors_dark,
                            proto_colors_light=proto_colors_light, ...)
```

**`web/templates/index.html`**: 3가지 변경.

1) FOUC(테마 깜빡임) 방지 — 첫 페인트 전에 `localStorage`에서 테마를
읽어 `<html data-theme>`를 미리 설정:

```html
<link rel="stylesheet" href="{{ asset_url('css/style.css') }}">
<script>
  (function () {
    var t = localStorage.getItem("compileTheme") || "dark";
    document.documentElement.setAttribute("data-theme", t);
  })();
</script>
</head>
<body>

<button id="theme-toggle-btn" class="theme-toggle-btn" type="button" aria-label="테마 전환"></button>
```

2) `proto-data` JSON 블록이 색 하나 대신 두 팔레트를 담게 변경:

```html
<!-- 수정 전 -->
"colors": {{ proto_colors | tojson }},

<!-- 수정 후 -->
"colorsDark": {{ proto_colors_dark | tojson }},
"colorsLight": {{ proto_colors_light | tojson }},
```

**`web/static/css/style.css`**: 라이트 테마 변수 세트 + 스캔라인
텍스처 제거 + 토글 버튼 스타일.

```css
/* 밝은/파스텔 테마 -- "컴파일러 터미널" 콘셉트는 그대로 두고 어두운
   배경/네온 텍스트만 밝은 종이 톤 + 차분한 세이지그린으로 바꾼다. */
:root[data-theme="light"] {
  --bg-void: #ece5d6;
  --bg-panel: #f5efe2;
  --bg-panel-raised: #faf6ec;
  --bg-card-back: #ffffff;
  --line-hair: rgba(60, 45, 20, 0.12);
  --phosphor: #17915f;
  --phosphor-dim: #6fae8d;
  --phosphor-glow: rgba(23, 145, 95, 0.18);
  --amber: #b5720c;
  --danger: #c62f47;
  --text-hi: #2b2620;
  --text-mid: #6b6255;
  --text-dim: #a89f8f;
}
```

다크 테마의 CRT 스캔라인 배경 텍스처(`repeating-linear-gradient`)는
파스텔 무드와 안 맞아서, 라이트 테마에서는 옅은 세이지그린 글로우만
남기고 뺐다:

```css
:root[data-theme="light"] body {
  background-image:
    radial-gradient(ellipse 80% 60% at 50% -10%, rgba(23,145,95,0.07), transparent);
}
:root[data-theme="light"] ::-webkit-scrollbar-thumb { background: #d8cfb9; }
```

토글 버튼(초기 버전 — 위치는 §4에서 재조정됨):

```css
.theme-toggle-btn {
  position: fixed;
  top: 14px;
  right: 14px;
  z-index: 40;
  font-family: var(--font-mono);
  font-size: 11px;
  letter-spacing: 0.5px;
  padding: 8px 12px;
  border-radius: var(--radius);
  border: 1px solid var(--line-hair);
  background: var(--bg-panel-raised);
  color: var(--text-mid);
  transition: border-color .15s, color .15s;
}
.theme-toggle-btn:hover { border-color: var(--phosphor-dim); color: var(--text-hi); }
.theme-toggle-btn::before { content: "☀ 파스텔 모드"; }
:root[data-theme="light"] .theme-toggle-btn::before { content: "● 다크 모드"; }
```

버튼 라벨을 JS가 아니라 `::before { content }`로 처리해서, 테마가
바뀌면 CSS 선택자 매칭만으로 라벨이 자동 전환되게 했다(JS는 상태
전환만 담당).

**`web/static/js/app.js`**: 테마 상태 관리 + 화면 재렌더링.

```js
let lastDraftState = null;   // renderDraft()가 매번 받는 state -- 테마 전환 시 재렌더링용

let currentTheme = document.documentElement.getAttribute("data-theme") || "dark";

function applyProtoPaletteForTheme() {
  PROTO.colors = currentTheme === "light" ? PROTO.colorsLight : PROTO.colorsDark;
}
applyProtoPaletteForTheme();

function setTheme(theme) {
  currentTheme = theme;
  document.documentElement.setAttribute("data-theme", theme);
  localStorage.setItem("compileTheme", theme);
  applyProtoPaletteForTheme();
  // 지금 보이는 화면을 새 팔레트로 다시 그린다 -- 진행 중인 게임/드래프트를
  // 새로고침 없이 유지한 채로 프로토콜 색만 갱신하려는 것.
  if (lastState) {
    render(lastState);
    if (lastState.pending && lastState.pending.kind === "input") {
      renderPrompt(lastState);
    }
  }
  if (lastDraftState) renderDraft(lastDraftState);
}

document.getElementById("theme-toggle-btn").addEventListener("click", () => {
  setTheme(currentTheme === "light" ? "dark" : "light");
});
```

`renderDraft(state)` 맨 앞에 `lastDraftState = state;` 한 줄을 추가해서
드래프트 화면에서도 테마 전환 시 재렌더링할 수 있는 재료를 확보했다.

**설계 포인트 — "새로고침 없이 진행 중인 게임 유지"**: 처음엔 `location.reload()`로
간단히 처리하려 했지만, 이 앱은 게임 세션을 URL/localStorage에
저장하지 않아서(`gameId`가 순수 메모리 변수) 새로고침하면 진행 중인
게임이 그냥 사라진다는 걸 코드에서 확인했다 — 그래서 `lastState`/
`lastDraftState`를 캐싱해뒀다가 테마 전환 시 그걸로 다시 그리는
방식을 택했다. `renderPrompt(state)`는 `state.pending.req`를 바로
읽어서 `pending`이 `null`이거나 `anim`이면 예외가 나므로, `pending.kind
=== "input"`일 때만 호출하도록 방어했다.

### 3.5 검증

Playwright(최초 설치, `npx playwright install chromium`)로 헤드리스
브라우저를 띄워 직접 확인했다:
- 랜딩 화면에서 토글 클릭 → `data-theme` 속성이 `dark`→`light`로 바뀜
- 실제 게임 진행 중(턴 2, 로그 누적된 상태)에 토글 클릭 → 프로토콜
  색만 파스텔로 바뀌고 턴/로그 등 게임 상태는 그대로 유지됨을 스크린샷
  두 장(라이트/다크 재전환)으로 확인
- 콘솔 에러 없음, 전체 pytest 스위트 영향 없음(웹 쪽은 pytest 대상이
  아니라 별도지만 회귀 없음을 `python -c "import web.app"`로 재확인)

---

## 4. 테마 토글 버튼 위치 조정

"색깔 모드 변경하는 버튼을 우측하단에 놓자"는 요청. 처음엔 단순히
`top: 14px` → `bottom: 14px`로 바꿨는데, 보드 화면에서 스크린샷으로
확인해보니 **하단의 프로토콜 벨트(`.proto-belt`, 화면 맨 아래 라인별
프로토콜 이름/값을 보여주는 줄)와 겹쳐서** 오른쪽 끝 프로토콜 pill이
가려지는 문제를 발견했다.

```css
/* 1차 시도 (겹침 발생) */
.theme-toggle-btn {
  position: fixed;
  bottom: 14px;
  right: 14px;
  ...
}
```

```css
/* 최종 수정 */
.theme-toggle-btn {
  position: fixed;
  bottom: 84px; /* 보드 화면 하단의 프로토콜 벨트(.proto-belt)와 안 겹치게 */
  right: 14px;
  ...
}
```

`.proto-belt`가 `flex: 0 0 auto`로 내용물 높이만큼(패딩 포함 약
60~65px)을 차지하는 걸 CSS에서 확인하고, 그보다 넉넉하게 띄운 값으로
재조정했다. Playwright로 다시 스크린샷을 찍어 겹침이 사라진 걸
확인했다.

---

## 5. AI 턴 진행 속도(페이싱) 조정

### 5.1 문제

"AI 행동이 너무 순식간에 일어나서, 플레이어 입장에서 무슨일이
일어났는지 모르겠어."

### 5.2 원인 분석

`app.js`에는 이미 서버가 보내는 애니메이션 이벤트(`state.pending.kind
=== "anim"`)마다 일정 시간 `sleep()`한 뒤 다음 스텝으로 진행하는
페이싱 로직이 있었다:

```js
// 수정 전
if (state.pending.kind === "anim") {
    showPromptBar(false);
    const ev = state.pending.event || {};
    if (ev.kind === "revealHand") { ... }
    else if (ev.kind === "rearrange") {
      ...
      const dur = Math.max(500, (state.pending.dur || 0.3) * 1000);
      await sleep(dur);
    } else if (ev.kind === "compileStart" || ev.kind === "compileFlip") {
      animateCompileFill(ev.player, ev.line);
      await sleep(950);
    } else {
      ...
      const dur = Math.max(300, (state.pending.dur || 0.3) * 1000 * 0.6);
      await sleep(dur);
    }
    ...
}
```

문제는 이 페이싱이 **사람 턴과 AI 턴을 구분하지 않고 똑같이
적용**된다는 것이었다 — 사람은 자기가 방금 뭘 했는지 이미 알고
있어서 빠르게 지나가도 되지만, AI 턴은 그 사이에 무슨 일이 일어났는지
로그/보드를 처음 보고 읽어야 하는데도 같은 속도(대부분 300~500ms)로
지나갔다.

### 5.3 수정

`state.players[pi].isAI`가 이미 서버 상태에 있다는 걸 확인하고,
`state.turn`(지금 턴 주인)이 AI면 각 스텝의 대기 시간에 배율(2.3배)을
곱하도록 바꿨다:

```js
// 수정 후
if (state.pending.kind === "anim") {
    showPromptBar(false);
    const ev = state.pending.event || {};
    // 지금 턴의 주인이 AI면 한 스텝 한 스텝을 더 오래 보여준다 -- 사람 턴은
    // 이미 자기가 뭘 했는지 아니까 빠르게 지나가도 되지만, AI 턴은 그 사이에
    // 무슨 일이 일어났는지 로그/보드를 읽을 시간이 따로 필요하다.
    const turnPlayer = state.players[String(state.turn)];
    const aiTurnPace = turnPlayer && turnPlayer.isAI ? 2.3 : 1;

    if (ev.kind === "revealHand") { ... }
    else if (ev.kind === "rearrange") {
      ...
      const dur = Math.max(500, (state.pending.dur || 0.3) * 1000) * aiTurnPace;
      await sleep(dur);
    } else if (ev.kind === "compileStart" || ev.kind === "compileFlip") {
      animateCompileFill(ev.player, ev.line);
      await sleep(950 * aiTurnPace);
    } else {
      ...
      const base = Math.max(300, (state.pending.dur || 0.3) * 1000 * 0.6);
      // AI 턴은 바닥값도 따로 올린다 -- base 자체가 아주 작은 이벤트는
      // *2.3을 해도 여전히 눈 깜짝할 새에 지나갈 수 있어서.
      const dur = turnPlayer && turnPlayer.isAI ? Math.max(650, base * aiTurnPace) : base;
      await sleep(dur);
    }
    ...
}
```

`state.turn`을 기준으로 삼은 이유: 개별 이벤트(`draw`/`flip`/`move`
등)마다 "이걸 누가 일으켰는지"를 나타내는 필드명이 이벤트 종류마다
다르거나 아예 없는 경우가 있어서(`flip` 이벤트엔 `player` 필드가
없음), 이벤트 단위가 아니라 **"지금이 누구 턴인가"**라는 훨씬
일관되고 항상 존재하는 필드를 기준으로 삼는 게 안전했다.

### 5.4 검증

Playwright로 서버 응답(`/api/advance_anim/`) 타임스탬프를 직접
측정했다(왕초보=`RandomAI`로 AI를 선공시켜, 즉시 결정하는 AI라
"결정 자체에 걸리는 시간"이 아니라 순수 애니메이션 페이싱만
관찰되게 함):

```
advance_anim 호출 수: 13
호출 간 간격(ms): [708, 713, 711, 701, 710, 724, 717, 725, 331, 316, 319, 339]
```

앞 8개(AI 턴, 평균 ≈714ms)와 뒤 4개(사람 턴으로 전환된 뒤, 평균
≈326ms)가 뚜렷하게 갈렸다 — 비율 714/326 ≈ 2.19배로, 의도한
2.3배와 거의 일치했다(부동소수/네트워크 오버헤드 수준의 오차).
사람 턴은 기존 속도 그대로 유지되고 AI 턴만 약 2.2배 느려진 것을
실측으로 확인했다.

---

## 6. "상대 손패" 표시가 AI 턴 중 뒤바뀌는 버그 수정

### 6.1 문제

"지금 화면 상단에는 상대방의 손패가 몇장인지가 표시되는데, ai와
대전할때는 항상 ai의 손패가 몇장인지 표시되게 하자." — 이 요청 자체가
"지금은 항상 그렇지 않다"는 관찰에서 나온 것이었다.

### 6.2 근본 원인

```js
// 수정 전
function currentSelfPi(state) {
  const req = state.pending && state.pending.kind === "input" ? state.pending.req : null;
  return req ? req.chooser : state.turn;
}

// 상단: "상대"(=지금 결정하는 사람이 아닌 쪽) 손패를, 내 손패와 동일한
// 크기의 뒷면 카드로 표시 (5번 요청: 크기 통일).
function renderOppHandPreview(state) {
  const self = currentSelfPi(state);
  const opp = self === 1 ? 2 : 1;
  const hand = state.players[String(opp)].hand;
  ...
}
```

`currentSelfPi()`는 "지금 input을 기다리는 결정권자"가 있으면 그
사람을, 없으면(`pending.kind === "anim"`일 때) `state.turn`(지금
턴 주인)을 "나"로 간주한다. 이 로직은 **hotseat 모드(둘 다 사람)엔
정확**하다 — 어느 시점이든 "지금 화면을 조작해야 할 사람"이 곧
"나"이기 때문이다.

문제는 **AI는 `input` 대기 없이 서버 안에서 즉시 결정을 내린다**는
점이다(`engine.py`의 `prompt()`가 `chooser.isAI`면 `coroutine.yield`
없이 바로 `ai.decide()` 결과를 반환). 그래서 AI 턴이 애니메이션으로
재생되는 동안(`pending.kind === "anim"`)엔 `currentSelfPi()`가
`state.turn`(=AI의 자리 번호)을 반환하게 되고, `opp = self===1?2:1`
계산 결과 **"상대"가 거꾸로 사람 자신의 자리**가 돼버린다 — AI
턴 애니메이션이 재생되는 동안 화면 상단엔 (사람이 이미 알고 있는)
자기 손패 장수가 표시되고 있었다.

### 6.3 수정

```js
// 수정 후
// 상단: "상대" 손패를, 내 손패와 동일한 크기의 뒷면 카드로 표시 (5번 요청:
// 크기 통일). vs_ai 모드에서는 항상 AI 쪽을 "상대"로 고정한다 -- AI는
// input pending 없이(prompt()에서 바로 inline 결정) 답하므로, AI 턴이
// 진행되는 동안엔 state.pending이 "anim"이라 currentSelfPi()가
// state.turn(=AI 자리)으로 폴백해서 "상대"가 거꾸로 사람 자신이 돼버리는
// 문제가 있었다(그 순간엔 이미 아는 자기 손패가 표시됨). AI가 없는
// hotseat에서는 기존처럼 "지금 결정하는 사람이 아닌 쪽"을 그대로 쓴다.
function renderOppHandPreview(state) {
  const aiSeat = [1, 2].find((pi) => state.players[String(pi)].isAI);
  const opp = aiSeat || (currentSelfPi(state) === 1 ? 2 : 1);
  const hand = state.players[String(opp)].hand;
  ...
}
```

`state.players[pi].isAI`로 AI 좌석을 직접 찾아서, 있으면(vs_ai 모드)
그 좌석을 무조건 "상대"로 고정한다. AI가 없는 hotseat 모드에서는
`aiSeat`가 `undefined`(falsy)라 기존 동적 계산으로 자연스럽게
폴백된다 — 즉 hotseat 동작은 전혀 안 바뀐다.

`currentSelfPi` 자체는 이 파일 전체에서 이 함수 한 곳에서만 쓰이고
있어서(재사용 지점이 하나뿐이라 파급 범위가 명확했다), 수정 범위를
`renderOppHandPreview` 내부로 좁게 가둘 수 있었다.

### 6.4 검증

Playwright로 서버 응답을 가로채서, AI 턴이 진행되는 동안(`turn: 2`,
AI 좌석) 400ms 간격으로 6번 샘플링해 **화면에 보이는 카드 수**와
**서버가 보고하는 AI의 실제 손패 수**를 비교했다:

```json
[
 {"shown": 5, "trueLen": 5, "turn": 2, "match": true},
 {"shown": 5, "trueLen": 5, "turn": 2, "match": true},
 {"shown": 5, "trueLen": 5, "turn": 2, "match": true},
 {"shown": 5, "trueLen": 5, "turn": 2, "match": true},
 {"shown": 5, "trueLen": 5, "turn": 2, "match": true},
 {"shown": 5, "trueLen": 5, "turn": 2, "match": true}
]
```

6개 샘플 전부 일치. 콘솔 에러 없음. 전체 pytest 스위트(390개)도
영향 없이 통과.

---

## 7. 기타 (코드 변경 없는 질의응답)

- **"휴리스틱이랑 ismcts 끼리 붙으면 상대승률이 어느정도였지?"** — 전날
  실측 결과를 재정리해 답변만 함(코드 변경 없음). 요지: 가장 신뢰도
  높은 기준값은 `iterations=200`/`rollout_turn_cap=24`/30판 기준
  **90.0% ±10.5(유의미)**, 현재 기본값(`rollout_turn_cap=2`)으로는
  아직 표본이 작아(16판) **75.0% ±26.2**로 방향은 이기는 쪽이지만
  통계적으로는 미확정 상태.
- **"앞으로 뭘 해야할까"** — 지금까지 진행 상황을 트랙별로(엔진 성능,
  학습된 평가함수 후속, 웹 난이도 확장, 직접 플레이하며 버그 찾기)
  정리해 답변. 아래 §8 로드맵에 반영.

---

## 8. 앞으로의 로드맵

### 8.1 엔진 성능 (가장 근본적, 가장 큰 작업)

- [ ] **`prompt()`/`emit()`을 스레드+큐 대신 파이썬 제너레이터(`yield`)
      기반으로 재작성.** 어제 `engine.lua`/`ai_mcts.lua`/`ai_sim.lua`
      원본을 확인하며 발견한 근본 원인: 우리 `Engine`은 매 판(그리고
      `ISMCTSAI`의 매 반복마다 만드는 복제본까지)마다 **진짜 OS
      스레드**를 띄우고 모든 yield를 **블로킹 `Queue.get()/put()`**으로
      처리하는데, Lua는 네이티브 코루틴(`coroutine.yield`/`resume`,
      같은 스레드 안에서 스택만 바꾸는 사실상 공짜 연산)을 쓴다.
      `rollout_turn_cap`을 24→12→4→2로 계속 낮춰온 것도 사실 이 근본
      비용을 우회하는 임시방편이었다 — 이 리팩터링이 되면 ISMCTS
      속도가 구조적으로 개선되고, 지금은 부담스러운 웹 "고급" 난이도
      (더 많은 `iterations`)도 현실적이 된다. 다만 엔진 핵심부를
      건드리는 만큼 회귀 위험이 커서 별도 계획 문서부터 신중하게
      시작해야 한다.

### 8.2 학습된 평가함수 후속 (`ai_regularization.md`/`ai_features_reference.md` §6)

- [ ] `rollout_turn_cap=2`의 실전 승률을 큰 표본(`n_pairs=30~50`)으로
      재확인 — 지금 75.0% ±26.2는 표본이 작아 확정이 아님.
- [ ] 더 큰 자기대국 데이터로 교차항(4850차원) 모델 재학습 — 지금은
      정규화 튜닝만으로 plain과 통계적으로 대등(49.0% ±7.1)한 데
      그쳤는데, 표본을 늘리면 더 안정적으로 추정된 계수로 plain을
      실제로 앞설 여지가 있는지 확인.
- [ ] `L1`이 살린 소수의 교차항(383/4850)이 실제로 어떤 특징쌍인지
      도메인 지식으로 해석 — 흥미로운 패턴이 나오면 손튜닝
      `evaluate()`에 그 교차항 하나를 손으로 추가하는 것도 가능.
- [ ] 특징 추가 후보: 내 남은 덱의 가치 정보, 패시브로 인한 라인 값
      왜곡폭, Control 재배치 실효성(`plan_rearrange` 탐침).

### 8.3 웹 난이도 확장

- [ ] "중급"(`ISMCTSLearnedAI`) 위의 "고급" 티어 — §8.1의 엔진 성능
      문제가 풀리기 전엔 응답 속도가 부담스러워 보류하는 게 나을 수
      있음.

### 8.4 지속적으로: 직접 플레이하며 버그 찾기

- [ ] 오늘 발견한 두 버그(`Smoke_0`/`Life_0` AI 채점, 상대 손패 표시)
      모두 코드 리뷰가 아니라 **실제로 플레이하다가** 나온 것들이었다
      — 이 방식이 의외로 효율적이었으므로, 시간 날 때마다 다양한
      프로토콜 조합으로 플레이해보며 비슷한 종류의 버그(카드 효과
      자체는 맞는데 AI 채점 로직이 조건을 놓치는 패턴 등)가 더
      있는지 계속 확인할 가치가 있다.

---

## 9. 학습된 eval 기준 하이퍼파라미터 재점검 (`ISMCTSLearnedAI`)

### 9.1 배경

`ISMCTSAI`의 하이퍼파라미터 기본값(`iterations=200`, `c_ucb=1.41`
(=√2, 표준 UCB1 상수), `rollout_turn_cap=2`)은 전부 **손튜닝
`evaluate()`**를 쓰던 시절에 스윕해서 정한 값들이다. 그런데 지금은
`ISMCTSLearnedAI`(학습된 평가함수, 손튜닝 대비 86.7% ±11.6로 유의미
우세 확인됨)로 갈아탔다 — 평가함수의 로짓 스케일과 판별력이 달라지면,
탐색을 얼마나 깊게/넓게 해야 최선인지도 같이 달라질 수 있다는
가설로, "학습된 eval 기준으로 하이퍼파라미터를 재점검해보자"는
제안을 사용자가 승인해 진행했다.

### 9.2 스윕 스크립트와 방법론 교정

`scripts/sweep_ismcts.py`(손튜닝 eval 스윕 때 쓰던 스크립트)를 본떠
`scripts/sweep_ismcts_learned.py`를 새로 작성했다. 축 3개
(`rollout_turn_cap`/`iterations`/`c_ucb`)를 한꺼번에 전수조사하기엔
판당 비용이 커서, **단계적으로(한 축씩) 스윕하고 앞 단계에서 가장
좋았던 값을 다음 단계의 고정값으로 삼는 방식**을 택했다.

**첫 시도(잘못된 방법론)**: 손튜닝 스윕 스크립트의 패턴을 그대로
따라, 후보 설정들을 전부 `HeuristicAI` 상대로 붙여봤다. 스모크
테스트(`n_pairs=1`)로 확인해보니 **하이퍼파라미터를 뭘로 바꿔도
`ISMCTSLearnedAI`가 `HeuristicAI`를 거의 항상 100% 이겨서, 설정 간
차이를 구분할 신호가 전혀 나오지 않았다** — `ISMCTSLearnedAI` 자체가
`HeuristicAI`보다 압도적으로 강해서, 상대가 너무 약하면 하이퍼파라미터
차이가 승패에 반영되지 않는 것.

**수정한 방법론**: 각 후보 설정을 `HeuristicAI`가 아니라 **"지금
기본값"(`cap=2, it=200, c_ucb=1.41`) 자기 자신과 직접 맞대결**시키는
구조로 바꿨다. 둘 다 비슷하게 강하므로 하이퍼파라미터 차이가 실제
승률 차이로 드러난다.

```python
# scripts/sweep_ismcts_learned.py 핵심 구조
BASELINE = {"iterations": 200, "c_ucb": 1.41, "rollout_turn_cap": 2}

def _make(cfg):
    return lambda: ISMCTSLearnedAI(iterations=cfg["iterations"], c_ucb=cfg["c_ucb"],
                                    rollout_turn_cap=cfg["rollout_turn_cap"])

# 각 후보를 baseline과 직접 맞대결 (arena()는 기존 scripts/arena.py 재사용,
# 미러 페어링 + 95% CI 계산 방식은 이전 스윕들과 동일)
r = arena(_make(cfg), _make(BASELINE), n_pairs=n_pairs, seed0=seed0, ...)
```

전 구간 표본은 `n_pairs=6`(12판, 6쌍 미러) 기준으로 진행했다(양쪽 다
`ISMCTSLearnedAI`라 판당 비용이 `vs-HeuristicAI`보다 훨씬 크다 —
판당 약 40~80초).

### 9.3 Phase A — `rollout_turn_cap` (고정값: `it=200, c_ucb=1.41`)

| 후보 | 승률(기준값 상대) | ±CI | 유의미 |
|---|---|---|---|
| `cap=4` | 50.0% (6승6패, 정확히 동률) | ±0.0 | - |
| `cap=8` | 58.3% (7승5패) | ±30.1 | - (낌새만) |
| `cap=12` | 16.7% (2승10패) | ±20.7 | **O, 기준값이 우세** |

`cap=12`는 확실히 나쁨. `cap=4`/`cap=8`은 기준값(`cap=2`)과 통계적으로
구분 안 됨. **결론: `cap=2` 유지**, Phase B의 고정값으로 채택.

### 9.4 Phase B — `iterations` (고정값: `cap=2, c_ucb=1.41`)

| 후보 | 승률(기준값 상대) | ±CI | 유의미 |
|---|---|---|---|
| `it=100` | 33.3% (4승8패) | ±20.7 | - (약해지는 쪽) |
| `it=150` | 58.3% (7승5패) | ±30.1 | - (낌새만) |
| `it=300` | 41.7% (5승7패) | ±30.1 | - (오히려 약해지는 쪽) |

`it=300`이 오히려 약한 쪽으로 나온 건 손튜닝 eval 스윕 때도 봤던
"과하게 늘려도 한계효용 없음" 패턴과 일치한다. **결론: `it=200`을
뒤집을 근거 없음, 유지**. Phase C의 고정값으로 채택.

### 9.5 Phase C — `c_ucb` (고정값: `cap=2, it=200`)

| 후보 | 승률(기준값 상대) | ±CI | 유의미 |
|---|---|---|---|
| `c_ucb=0.7`(Lua 값) | 75.0% (9승3패) | ±33.5 | - (낌새, 가장 강함) |
| `c_ucb=1.0` | 66.7% (8승4패) | ±32.7 | - (낌새) |
| `c_ucb=2.0` | 41.7% (5승7패, 기준값이 58.3%로 우세) | ±30.1 | - |

`0.7 → 1.0 → 1.41(기준) → 2.0` 순서로 승률이 **정확히 단조 감소**하는
패턴이 눈에 띄었다 — 개별로는 전부 무의미하지만, 4개 값에 걸쳐 이렇게
일관되게 한 방향으로 정렬된 건 우연치고는 패턴이 뚜렷해 보였고,
공교롭게도 `c_ucb=0.7`은 Lua `ai_mcts.lua`가 독자적으로 튜닝해서 쓰던
`UCB_C` 상수와 정확히 같은 값이었다. 표본을 늘려 확정해볼 가치가
있다고 판단해 Phase D로 이어졌다.

### 9.6 Phase D — `c_ucb=0.7` 표본 확대 재확인

가장 유력한 후보 하나(`c_ucb=0.7`)만 골라 표본을 `n_pairs=6`→`15`(총
30판)로 5배 늘려 재확인했다.

```
c_ucb=0.7 (확대 재확인) vs baseline(cap=2, c_ucb=1.41)
  30판(15쌍 미러) · 1992.2초 · 판당 66406ms
  c_ucb=0.7 15승 / baseline 15승  -- 정확히 반반
  → 승률 50.0% ±13.5
  → 통계적으로 무의미
```

**Phase C에서 봤던 75.0%의 쏠림이 표본을 5배로 늘리자 완전히
사라지고 정확히 50.0%로 수렴했다** — 초반 신호는 소표본 노이즈였던
것으로 확정됐다. `c_ucb=1.0`/`cap=8`(58.3%)/`it=150`(58.3%) 등
Phase A~C에서 봤던 다른 "낌새"들도 재확인 없이는 신뢰할 수 없는,
같은 종류의 노이즈일 가능성이 높다는 걸 보여주는 결과이기도 하다.

### 9.7 최종 결론

4단계 전부 거친 뒤에도 `ISMCTSAI`의 기존 기본값
(`rollout_turn_cap=2`, `iterations=200`, `c_ucb=1.41`)을 뒤집을
유의미한 근거를 찾지 못했다 — **학습된 eval로 갈아탄 뒤에도 하이퍼
파라미터는 그대로 유지**하는 게 맞다는 결론. 손튜닝 eval 기준으로
잡았던 값들이 학습된 eval에도 우연히(혹은 두 eval의 판별 특성이
비슷해서 자연스럽게) 그대로 맞아떨어진 셈이다.

방법론적으로 남는 교훈: `n_pairs=6`(12판) 수준의 표본에서 나온 "낌새"
(±CI가 승률 자체와 맞먹거나 더 큰 경우, 예: 58.3%±30.1)는 방향성조차
신뢰하기 어렵고, 확정하려면 최소 `n_pairs=15`(30판) 이상으로 재확인이
필요하다 — 이번 스윕에서 가장 강했던 낌새(75.0%±33.5)조차 표본을
늘리자 완전히 사라졌다.

---

## 10. 학습된 eval 자기개선 루프(경량 expert iteration) 1세대 시도 — 실패로 확정

### 10.1 배경과 가설

지금 배포된 학습 eval(`eval_weights_plain.npz`, 이하 gen1)은 `HeuristicAI`
(앞보기 없이 태그 채점만) 자기대국 데이터로 학습됐다. `ai_regularization.md`
에서 97차원 plain과 4850차원 교차항 확장 모델이 이미 통계적으로 대등
(49.0% ±7.1)하다고 나온 건, "지금 데이터에서 뽑아낼 정보는 모델을
키워도 더 안 나온다"는 신호로 읽을 수 있다 — 그렇다면 남은 레버는
모델 용량이 아니라 **데이터 자체의 질**이다.

가설: `HeuristicAI`보다 판단력이 나은 정책으로 자기대국을 두면, 더
현실적인(실제로 벌어질 법한 강한 플레이 수준의) 상황들을 데이터가
담게 되고, 그걸로 다시 학습한 eval(gen2)이 gen1보다 나아질 것이다 —
본격 정책망/가치망 RL 없이 지금 파이프라인을 반복 적용하는 경량
버전("경량 RL이 유일한 방법은 아니지 않냐"는 질문에 대한 대안으로
제시했던 방법, 사용자 승인 후 진행).

### 10.2 설계 — 왜 ISMCTS가 아니라 `LookaheadAI`로 자기대국을 뒀나

`ISMCTSLearnedAI` 자기대국(풀 트리 탐색, `iterations=200`)으로 데이터를
새로 뽑는 건 판당 비용이 너무 크다(아레나 실측: `ISMCTSLearnedAI` vs
`ISMCTSLearnedAI` 판당 약 60~70초 — 4000판이면 며칠 단위). 대신 이미
있는 `src/game/ai_lookahead.py`의 `LookaheadAI`를 썼다 — 트리 탐색 없이
후보 액션마다 딱 한 번씩 시뮬레이션해서 결과를 평가하는(`ai_sim.pick_best`,
`samples=1`) 훨씬 저렴한 방식인데, 이미 `eval_fn`/`eval_w` 인자를
지원해서 `evaluate_learned` + gen1 가중치를 그대로 꽂을 수 있었다:

```python
LookaheadAI(eval_fn=evaluate_learned, eval_w=load_eval_weights(gen1_path))
```

`HeuristicAI`의 정적 우선순위 채점보다 근거 있는 선택(실제로 시뮬레이션해
보고 고름)을 하면서도 풀 ISMCTS보다는 훨씬 싸다.

### 10.3 `RecordingAI`를 상속 대신 위임 구조로 리팩터링

`scripts/generate_selfplay_data.py`의 `RecordingAI(HeuristicAI)`는
`HeuristicAI`만 감쌀 수 있게 상속으로 짜여 있었다. 어떤 정책 AI든
감쌀 수 있게 위임(composition) 구조로 바꿨다:

```python
class RecordingAI:
    """내부 정책 AI(HeuristicAI든 LookaheadAI든 무엇이든)를 감싸서, 매
    'action' 프롬프트 직전에 두 플레이어 시점 특징 벡터를 같이 적어둔다.
    실제 결정은 전부 내부 AI(inner)에 위임한다."""
    def __init__(self, inner):
        self.inner = inner
        self.snapshots = []

    def decide(self, g, req):
        if req.get("type") == "action":
            self.snapshots.append((1, extract(g, 1)))
            self.snapshots.append((2, extract(g, 2)))
        return self.inner.decide(g, req)

    def __getattr__(self, name):
        return getattr(self.inner, name)  # planRearrange 등 위임
```

`play_one_game`/`generate`에 `policy_factory` 파라미터를 추가(기본값
`HeuristicAI`, 기존 동작과 100% 동일)하고, CLI에
`--policy lookahead --weights <경로>` 옵션을 추가했다. 리팩터링 직후
`pytest tests/test_generate_selfplay_data.py`로 기존 6개 테스트가 전부
그대로 통과하는지 확인해 회귀가 없음을 검증했다.

### 10.4 파일럿 — 판당 비용 실측

전체 규모를 정하기 전에 `LookaheadAI` 자기대국 30판으로 먼저 비용을
쟀다(예상보다 비쌀 수 있다는 우려가 있었음 — 클론마다 스레드+큐
오버헤드가 있는 엔진 구조 때문):

```
완료: 30/30판 정상 종료 (117.0초)
샘플 2786개 (판당 평균 92.9개)
```

**판당 약 3.9초** — 우려했던 것보다 훨씬 쌌다(`HeuristicAI` 자기대국의
판당 샘플 밀도 86.6개와 비슷한 수준이라 게임 길이 자체는 비슷했다는
뜻). 이 실측치로 전체 데이터 규모를 2000판(예상 약 2시간)으로 정했다
— gen1의 4000판보다 작지만, 판당 샘플 밀도가 비슷하면 약 18만 개
샘플이 나올 거라 97차원 선형 모델 학습엔 충분하다고 판단했다.

### 10.5 gen2 데이터 생성 및 학습

```
완료: 2000/2000판 정상 종료 (8804.6초 ≈ 2시간27분)
샘플 184170개 (판당 평균 92.1개)
승/패 비율: 50.0% / 50.0%   -- 다양성 주입 덕에 좌우 대칭, 예상대로
```

`scripts/train_eval.py`로 plain(97차원, `C=1.0` 기본, 교차항 확장
안 함 — `ai_regularization.md`에서 이미 확장이 이득 없다고 확인했으므로)
학습:

```
학습 로그손실/정확도: 0.5783 / 68.7%
검증 로그손실(logloss): 0.5819
검증 정확도(accuracy) : 68.6%
0이 아닌 계수 비율: 99.0%
```

**여기서 첫 번째 경고 신호**: gen1의 검증 로그손실(0.5708)보다 gen2가
오히려 더 나쁘다(0.5819, +0.0111). 다만 두 수치는 서로 다른 데이터셋
(gen1=`HeuristicAI` 자기대국, gen2=`LookaheadAI` 자기대국)에서 잰
거라 직접 비교가 정확하지 않을 수 있다는 단서를 달아뒀다(예: `LookaheadAI`
쪽 자기대국이 더 팽팽한/예측하기 어려운 상황까지 도달하는 경향이라면,
모델이 똑같이 좋아도 로그손실 자체는 더 높게 나올 수 있음) — 그래서
이 지표만으로 판정하지 않고 계획대로 아레나 검증까지 진행했다.

가중치 파일을 저장하기 전에 `eval_scale`도 gen2 전용으로 재실측했다
(gen1의 `eval_scale=2.0`을 그대로 재사용하면 안 됨 — 다른 가중치는
로짓 분포가 다르므로):

```python
# selfplay_gen2.npz의 X 전체에 gen2 가중치를 곱해서 로짓 분포 측정
mean = -0.0013, std = 1.2006, p95 = 2.512   (gen1: 평균≈0, std≈1.16, p95≈1.9)
```

gen1과 같은 방식(p95 근사)으로 `eval_scale=2.5`를 역산해서 아레나에 썼다.

### 10.6 아레나 검증 — 무의미, 결론: 채택 안 함

```
ISMCTSLearnedAI(gen2, eval_scale=2.5) vs ISMCTSLearnedAI(gen1, eval_scale=2.0)
하이퍼파라미터: 둘 다 cap=2/it=200/c_ucb=1.41 (4단계 스윕 확정값, 안 건드림)

30판(15쌍 미러) · 1475.9초 · 판당 49198ms
gen2 14승 / gen1 16승
→ gen2 승률 46.7% ±17.8
→ 통계적으로 무의미 (오히려 gen1 쪽으로 살짝 기움)
```

학습 단계의 로그손실 신호(gen2가 약간 나쁨)와 아레나 결과(gen2가
살짝 열세, 무의미)가 **같은 방향**을 가리켰다 — 서로 다른 두 방법이
같은 결론에 도달했다는 점에서, 단순 노이즈보다는 신뢰할 만한 부정적
신호로 판단했다.

**결론: gen2는 채택하지 않는다. `ISMCTSLearnedAI`의 기본 가중치는
계속 gen1(`eval_weights_plain.npz`)로 유지한다.** 이번 세대의 자기개선
루프는 여기서 중단 — 계획서(`ai_train_pipeline.md` 후속,
`C:\Users\nicy1\.claude\plans\parallel-tumbling-mccarthy.md`)에 이미
명시해뒀던 "무의미하거나 패배하면 중단, 실패가 아니라 유효한 결론"
분기를 그대로 따른 것이다.

### 10.7 왜 안 통했을까 — 사후 분석

미리 우려했던 구조적 한계가 그대로 들어맞은 것으로 보인다: `LookaheadAI`
자체가 **gen1의 eval로 판단**해서 자기대국을 두기 때문에, gen1이 이미
갖고 있는 맹점/편향된 판단은 데이터에도 그대로 반영된다 — "스스로를
스승 삼는" 부트스트랩의 전형적인 한계로, 외부의 더 정확한 신호(예:
훨씬 깊은 탐색, 실제 사람 기보, 다른 독립적인 평가 기준) 없이는 자기
안의 정보를 재배열할 뿐 새로운 판단력을 얻기 어렵다. `ai_regularization.md`
의 "97차원과 4850차원이 대등하다"는 결과와 겹쳐 보면, 지금 특징
집합(`ai_features.py`, 97차원)과 지금 정보 흐름(HeuristicAI 또는
LookaheadAI 자기대국) 조합으로는 이미 뽑아낼 수 있는 신호를 거의 다
뽑아낸 상태일 가능성이 크다.

### 10.8 남은 산출물과 후속 옵션

- 코드 변경(`RecordingAI` 위임 구조, `--policy lookahead` 옵션)은 **그대로
  유지** — 다음에 이 방향을 다시 시도할 때(예: 더 강한 정책, 또는
  ISMCTS 자체로 더 비싸더라도 데이터를 뽑는 실험) 바로 재사용 가능.
- `selfplay_gen2.npz`/`eval_weights_plain_gen2.npz`는 삭제하지 않고
  archive로 남겨둠(재현/비교용).
- 이 방향의 다음 시도가 통하려면 "gen1과 다른 판단 기준"이 필요해
  보인다 — 후보: (a) 훨씬 비싸더라도 `ISMCTSLearnedAI`(풀 탐색) 자기
  대국으로 데이터를 뽑기(엔진 성능 리팩터링이 선행돼야 현실적), (b)
  손튜닝 `evaluate()`와 학습 eval을 섞어서 자기대국을 두게 해 서로
  다른 두 판단 기준을 대조시키기, (c) 특징 집합 자체를 늘리기
  (§8.2에 이미 있는 후보: 덱 가치, 패시브 왜곡, Control 실효성).
  전부 이번 작업 범위 밖의 별도 후속 논의 대상.

---

## 11. Main3/Aux3 포팅 — 신규 프로토콜 15개, 카드 90장

### 11.1 배경과 범위

사용자가 Lua 원본 프로젝트의 Main3/Aux3 카드 데이터(영어 설명 + Lua
코드, 카드별로 정리된 텍스트 파일)를 첨부하며 포팅을 요청했다. 대상은
프로토콜 15개(Main3 12개 + Aux3 3개), 각 6장씩 총 90장:

| 세트 | 프로토콜 |
|---|---|
| Main 3 | Ambush(매복) · Envy(질투) · Fulcrum(받침점) · Gluttony(탐식) · Greed(탐욕) · Lust(색욕) · Momentum(탄력) · Nova(신성) · Overwhelm(압도) · Pride(교만) · Sloth(나태) · Wrath(분노) |
| Aux 3 | Flexible(유연함) · Inert(무기력) · Rigid(경직) |

기존 180장(30개 프로토콜)에서 **270장(45개 프로토콜)**으로 확장.
`protocols.py`/`carddefs.py`/`card_text.py`/`ai_prior.py` 네 파일을
동시에 확장해야 하는 구조(프로토콜 정의 → 카드 로직 → 한글 텍스트 →
AI 채점 태그)라, 포팅 자체보다 **이 카드들이 요구하는데 엔진에 아직
없던 기본 동작을 찾아 추가하는 일**이 작업의 대부분을 차지했다.

한글 카드 텍스트 자료를 두 번 받았는데 처음엔 인코딩이 깨진 채로
와서(여러 방식으로 복구 시도했지만 실패 — 단순 오인코딩이 아니라
데이터 자체가 손상된 상태였음) 일부(Ambush~Pride 10개 프로토콜, 60장)는
사용자가 다시 정확히 보내준 걸 썼고, 나머지(Sloth/Wrath/Flexible/
Inert/Rigid, 30장)는 영어 설명을 바탕으로 기존 `card_text.py` 문체에
맞춰 직접 번역했다.

### 11.2 엔진에 새로 추가한 기본 동작들

기존 180장은 필요로 한 적이 없던 메커닉들이라, `engine.py`에 다음을
새로 추가했다(전부 기본값을 기존 동작과 동일하게 둬서 하위 호환 유지):

**Control 획득/포기** (Lust_0/Lust_3/Lust_4/Envy_1/Nova_2/Pride_6/Wrath_1/
Wrath_4 등 10장 가까이가 필요로 함) — 기존엔 2라인 이상 지배 시
자동으로만 부여되는 경로(`check_control`)뿐이었고, 카드 효과가 직접
가져오거나/버리는 경로가 아예 없었다:

```python
def _set_control(self, pi):
    """Control을 pi에게 넘긴다. 자동 규칙(check_control)과 카드 효과의
    gain_control() 양쪽이 공유하는 경로 -- afterGainControl 반응
    트리거가 어느 경로로 왔든 일관되게 발동해야 하므로."""
    if self.control == pi:
        return False
    self.control = pi
    self.emit("control", {"player": pi, "i18n": {"key": "ev.controlTake", "params": {"p": pi}}})
    self.fire_reactive(pi, "afterGainControl")
    return True

def gain_control(self, pi):
    if self.effect_interrupted():
        return False
    return self._set_control(pi)

def lose_control(self, pi):
    """실제로 뭔가 바뀌었으면 True -- 호출부가 `if g.lose_control(pi):
    ...`로 후속 효과를 게이팅하는 계약."""
    if self.effect_interrupted() or self.control != pi:
        return False
    self.control = None
    self.emit("control", {"player": None, "i18n": {"key": "ev.controlLose", "params": {"p": pi}}})
    return True
```

**카드 단위 이동 금지** (`can_move`, Rigid_7 "뒤집거나 이동시킬 수
없다") — `can_flip`과 대칭으로 추가하고, `move_card()` 자체에서
강제하도록 함(호출부마다 따로 필터링 안 해도 항상 지켜짐):

```python
def can_move(self, card):
    return not card.definition.get("cantMove")

def move_card(self, card, to_player, to_line):
    if self.effect_interrupted():
        return False
    if not self.can_move(card):
        return False
    ...
```

**손 카드를 덱 맨 아래로** (`put_hand_card_on_deck_bottom`, Sloth_2).

**덱 맨 위 카드를 상대 스택에 얹는 교차 플레이** (Nova_0 "덱 맨 위
카드를 드러난 '신성' 카드 아래에 낸다" — 그 신성 카드가 상대 스택에
있을 수도 있음) — `play_top_face_down`에 `side` 파라미터 추가(기본값
`None`→`pi`, 기존 180장 동작 불변):

```python
def play_top_face_down(self, pi, line, under_card=None, side=None):
    ...
    side = pi if side is None else side
    ...
    e = self.commit(c, {"zone": "field", "pi": side, "line": line, "faceUp": False,
                         "underCard": under_card}, after_land)
```

**손 카드를 다른 카드 바로 밑에 끼워 넣기** (Rigid_3 "이 카드 바로
밑에 낸다") — `play_card`/`play_external`에 `under_card` 파라미터
추가. 실제 슬라이드-인 삽입 로직(`perform_landing`의 `underCard` 처리)
자체는 이미 존재했지만 지금까지 `play_top_face_down` 한 경로에서만
쓰이고 있었다는 걸 이번에 발견 — 손 카드 플레이 경로에도 그대로
연결하면 됐다.

**새 패시브 종류 3가지** — `line_value()`에 두 단계 추가:

```python
# 이 라인(양쪽 스택 통틀어) 최고값 카드를 총합에서 제외 (Wrath_0)
ignore_highest = any(c.face_up and c.definition.get("passive", {}).get("ignoreHighestInLine")
                      for side in (1, 2) for c in self.players[side]["stacks"][line])
...
# 이 라인 양쪽 모두 동일하게 오르는 패시브 (Lust_0, 라인 어느 쪽에 있어도)
for side in (1, 2):
    for c in self.players[side]["stacks"][line]:
        if c.face_up:
            bonus = c.definition.get("passive", {}).get("lineValueBoth")
            if bonus:
                total += bonus
```

`allowFaceUpHere`가 불리언 값을 가질 수 있게 확장(Lust_2 "내 카드는
프로토콜이 일치하지 않아도 이 스택에 플레이할 수 있다" — 기존
Unity_1은 특정 프로토콜 이름 문자열이었는데, Lust_2는 "이 카드
소유자 자신의 카드는 전부" 허용이라 값 종류가 다름):

```python
def _proto_allowed_here(g, line, proto, pi=None):
    for top in g.cards_with_def_field("allowFaceUpHere", {"line": line, "top_only": True}):
        flag = top.definition.get("allowFaceUpHere")
        if flag == proto:
            return True
        if flag is True and pi is not None and top.owner == pi:
            return True
    return False
```

`compilable_lines()`에 Control 기반 컴파일 봉쇄 추가(Lust_0 "내가
컨트롤을 가지고 있다면 상대는 컴파일할 수 없다"), `phase_trigger_
resolvable()`에 하단 명령 억제 추가(Inert_1 "이 라인의 다른 카드는
하단 명령이 없는 것으로 취급").

### 11.3 90장 포팅 구조

`carddefs.py`는 이미 180장짜리 공용 헬퍼(`_flip_one`/`_delete_one`/
`_move_one`/`_choose_extreme` 등)가 잘 갖춰져 있어서 대부분 재사용됐고,
Main3/Aux3 전용으로 새로 추가한 공용 헬퍼는: `_choose_mode`(둘 이상의
효과 갈래 중 선택, Flexible류), `_choose_extreme`+`_tied_extreme`(값
최대/최소 카드 선택, 동점이면 chooser가 고름), `_directly_above`(바로
위 카드), `_play_hand_face_down`/`_play_hand_face_down_each`(손 카드를
뒷면으로, 소유자 아닌 좌석에도 낼 수 있고 특정 카드 밑에도 낼 수 있음).

카드 90장은 원본 Lua 구조를 그대로 따라 프로토콜별 섹션으로 나눠
포팅했다. 예시(Nova_0, 가장 복잡한 카드 — 시작/중단/종료 3개 트리거
전부 사용):

```python
def _nova_0_start_top(g, c):
    lines = _exact_five_lines(g, c.owner)
    if not lines:
        return
    line = g.choose_line_from(c.owner, lines, {...}) or lines[0]
    cards = [card for pi in (1, 2) for card in g.players[pi]["stacks"][line] if card.face_up]
    g.delete_cards(cards)  # "모두 제거"는 단일 명령

def _nova_0_play(g, c):
    controller = g.control
    if controller:
        _swap_two(g, controller, c.owner)  # 컨트롤을 가진 쪽이 재배치

def _nova_0_finish(g, c):
    targets = _nova_under_targets(g, c.owner)
    if not targets:
        return
    target = g.choose_card_from(c.owner, targets, {...}) or targets[0]
    side, line, _idx = g.locate(target)
    if side is not None and line and g.can_play_face_down(c.owner, None, line)[0]:
        g.play_top_face_down(c.owner, line, under_card=target, side=side)  # 교차 플레이
```

`card_text.py`(한글 카드 텍스트 + 프로토콜명/태그라인/키워드)와
`ai_prior.py`(TAGS, AI 채점용)도 90장 전부 채웠다. AI 태그는 기존
관례(정밀 모델링이 어려운 조건부/Control 관련 카드는 `ongoing: True`로
보수적 처리)를 그대로 따랐다.

### 11.4 웹 UI 갱신

- **도감(Codex) MAIN 3 탭 추가**: `codexGroupOf()`/`renderCodexList()`가
  기존엔 `main1`/`main2` 두 그룹만 알고 있어서 신규 15개 프로토콜이
  사이드바에 아예 안 뜨던 문제 발견 → 탭/그룹 매핑 3-way로 확장.
- **밴픽 세트 선택을 단일선택→복수선택으로 개편**: 기존엔 "MAIN1+AUX1"
  / "MAIN2+AUX2" / "전체" 3개 버튼 중 하나만 고르는 구조였는데, 사용자
  요청으로 MAIN1/2/3을 각각 독립적으로 켜고 끌 수 있게 다시 짰다(기본
  전체 선택, 마지막 하나는 못 끄게 막음). 백엔드(`draftpool.py`)는
  이미 임의의 세트 조합을 지원하고 있어서 프론트만 고치면 됐다.

Playwright로 실제 브라우저에서 둘 다 검증(도감 MAIN3 탭에 15개
프로토콜 정상 표시, 밴픽 토글이 정확히 선택된 세트만 풀에 반영되는지
API 응답까지 확인). 이 과정에서 세션 훨씬 이전부터 떠 있던 낡은 Flask
프로세스가 포트를 물고 있어서 `index.html` 수정이 브라우저에 전혀
반영이 안 되고 있던 것도 발견 — 서버 재시작으로 해결(참고용 메모:
`flask run`은 `--debug` 없이는 템플릿 자동 재로드가 꺼져 있어서,
프로세스를 오래 띄워두고 템플릿만 고치면 이런 식으로 안 반영될 수
있음).

### 11.5 검증

- `pytest` 전체 스위트: 180장 시절 390개 → **405개**(신규 메커닉
  회귀 테스트 `test_carddefs_main3_mechanics.py` 12개 + 프라이버시
  테스트 2개 포함, 이후 §12 버그 수정으로 407개까지 늘어남).
- `RandomAI`/`HeuristicAI` 자기대국 스모크 테스트 총 200판 이상,
  45개 프로토콜 전체 조합 커버, 에러 0건.
- 웹앱 import, `ISMCTSLearnedAI`(학습 eval, 97차원 feature — 신규
  프로토콜과 무관하게 그대로 동작 확인) 정상.

---

## 12. 실전 플레이로 발견한 버그 4건

Main3/Aux3를 실제로 플레이해보며(코드 리뷰가 아니라) 발견한 문제들.
전부 사용자가 웹에서 직접 플레이하다가 리포트한 것들이고, 카드 룰
로직 자체가 아니라 **주변 인프라(웹 직렬화, AI 판단, 엔진 이벤트
배선)의 빈틈**이었다는 공통점이 있다 — 90장 각각의 `play`/`start`/
`finish` 함수는 자기대국 스모크 테스트로 이미 폭넓게 검증됐지만, 그
스모크 테스트는 웹 직렬화나 AI의 "다음 수 순서" 같은 건 건드리지
않기 때문에 놓친 것들.

### 12.1 색욕6 — 상대 손패 선택 화면에 카드 정보가 null로 표시됨

**증상**: 색욕6("카드를 1장 버립니다. 상대는 이 라인에 카드 1장을
뒷면으로 플레이합니다")을 내면, 상대가 손패에서 카드를 고르는 화면에
카드 이름/값이 전부 `null`로 나옴(스크린샷 확인).

**원인 1(표면)**: `_lust_6_play`가 공용 헬퍼(`_play_hand_face_down`)를
안 쓰고 `choose_card_from`을 직접 호출하면서 `dests` 필드를 안 넘김.

**원인 2(근본)**: `web/app.py`의 `_serialize()`가 손패 공개 여부를
`e.turn`(지금 누구 턴인지)만 보고 판단하고 있었다:

```python
# 수정 전
visible = e.turn == pi  # hotseat: 지금 자기 턴일 때만
```

색욕6/불활성3처럼 카드 효과가 **상대에게** "네 손패에서 골라라"를
강제하면, 실제 응답자(`pending.req.chooser`)는 카드 소유자가 아닌
쪽인데 `e.turn`은 여전히 카드를 낸 쪽이다. 그러니 정작 자기 손패에서
고르는 사람인데도 "지금 네 턴 아니잖아"로 취급돼 `proto`/`value`가
전부 `None`으로 나갔다.

```python
# 수정 후
pending_chooser = None
if e.pending and e.pending.get("kind") == "input":
    pending_chooser = e.pending.get("req", {}).get("chooser")
...
visible = (e.turn == pi) or (pending_chooser == pi)  # hotseat
```

`_serialize` 단위 테스트로 수정 전/후 데이터를 직접 비교해서 확인
(같은 상황을 만들어 `pending`을 주입하고 `hand[-1]["proto"]`가
`None`→실제 값으로 바뀌는지). 회귀 테스트 2개 추가
(`test_hotseat_reveals_hand_to_the_actual_chooser_of_a_pending_prompt`,
`test_hotseat_still_hides_hand_when_pending_is_not_an_input_prompt`).
불활성3도 같은 패턴(`_play_hand_face_down(g, opp, line, ...)`)이라
같이 고쳐졌다.

### 12.2 압도2 — AI가 자기 라인부터 채워서 효과가 조기 중단됨

**증상**: 압도2("종료: 각 라인에 내 덱 맨 위 카드를 뒷면으로
플레이합니다. 이 카드를 뒤집습니다.")를 발동시켜보면 3라인 중 1장만
놓이고 멈춤.

**원인**: 이건 사실 **공식 FAQ 룰이 의도대로 작동한 것**이었다 —
"각 라인마다" 효과 도중 그 효과를 낸 카드 자신이 다른 카드에
덮이면, 그 즉시 명령이 중단된다(이미 Life_0/Smoke_0에도 적용된
룰, `_play_top_face_down_each`의 `is_uncovered` 체크). 압도2는
자기 자신이 있는 라인도 "각 라인"에 포함되므로, 그 라인을 먼저
채우면 스스로를 덮어서 나머지 라인이 처리되기 전에 멈추는 게 맞다.

문제는 **AI가 이 사정을 전혀 모르고 항상 자기 라인부터 골랐다는 것**.
`ai_prior.py`의 `choose_line()`이 그냥 "지금 유리한 라인부터" 채점하다
보니, 자기 카드가 있는 라인이 유리하면(대개 그럼 — 자기 카드 값이
더해지니까) 매번 거기부터 채워서 스스로 효과를 반쯤 죽이고 있었다.

```python
if intent in ("play", "move"):
    # "각 라인마다 카드를 낸다" 류 효과가 라인을 하나씩 순서대로 물을 때,
    # 지금 발동 중인 카드 자신이 있는 라인을 맨 먼저 채우면 스스로를
    # 덮어버려서 남은 라인들이 처리되기도 전에 명령이 중단된다(공식 FAQ).
    # 다른 후보가 남아있는 한 그 라인은 맨 뒤로 미룬다.
    source_uid = req.get("sourceUid")  # engine.prompt()가 자동으로 채워줌

    def score_play(l):
        s = g.line_value(me, l) - g.line_value(o, l) * 0.3
        if source_uid is not None and len(cands) > 1:
            top = g.top_card(me, l)
            if top and top.uid == source_uid:
                s -= 1000
        return s
    return best(score_play)
```

`sourceUid`는 `engine.prompt()`가 "지금 resolve 중인 카드가 뭔지"를
`_card_stack`에서 자동으로 채워주는 필드라 별도 배선 없이 바로 쓸 수
있었다. `resolve_end_triggers()`(실제 종료 페이즈 경로, `run_card`로
감싸져 있어야 `_card_stack`이 채워짐 — 처음엔 `finishTop` 함수를
직접 호출하는 테스트를 짰다가 `sourceUid`가 항상 `None`이라 재현이
안 돼서 이 차이를 알아챘다)로 재현: 수정 전엔 1라인만, 수정 후엔
3라인 다 채워지되 자기 라인이 항상 마지막에 채워짐을 확인. 같은
헬퍼를 쓰는 Momentum_0/Overwhelm_1(그리고 기존 Life_0/Smoke_0)도
이번 수정으로 같이 개선됐다.

### 12.3 탄력1 — afterRearrange 리액티브가 애초에 발동한 적이 없었음

**증상**: 탄력1("플레이어가 프로토콜을 재배열한 뒤: 카드를 1장
버립니다. 카드를 1장 뽑습니다.")가 프로토콜 재배열 후에도 전혀
반응하지 않음.

**원인**: `afterCompile` 리액티브는 `do_compile()`에서 이미
`fire_reactive(pi, "afterCompile")`로 쏘고 있었는데, **`afterRearrange`는
`swap_protocols()`/`rearrange_protocols()` 어느 쪽도 한 번도 쏜 적이
없었다.** 실제로 프로토콜을 바꾸는 로직(`p["protocols"][a], p["protocols"][b]
= ...`)과 이벤트 emit은 있는데, 그 뒤에 리액티브 발화 코드가 통째로
빠져 있었던 것 — 기존 180장 중 `afterRearrange`를 쓰는 카드가 하나도
없어서 지금까지 드러나지 않은 엔진 차원의 누락이었다(§11.2의
`afterGainControl`과 같은 종류의 문제).

```python
def swap_protocols(self, pi, a, b):
    ...
    self.emit("rearrange", {...})
    self.fire_reactive(pi, "afterRearrange")  # 추가

def rearrange_protocols(self, pi, order):
    ...
    self.emit("rearrange", {...})
    self.fire_reactive(pi, "afterRearrange")  # 추가
```

`swap_protocols`(부분 교환)를 직접 호출해서 재현: 수정 전엔 버림더미
0장 그대로, 수정 후엔 정확히 1장 버려지고 다시 뽑아 손패 수는
그대로임을 확인. `rearrange_protocols`(전체 재배열) 경로도 별도로
테스트해서 두 메서드 다 똑같이 고쳐졌는지 확인. 신성2("내가
재배열한 뒤: 뒷면 카드를 이동할 수 있다")도 같은 이벤트를 쓰는
카드라 이번 수정으로 같이 고쳐졌다.

### 12.4 프로토콜 한글명 교정

번역 과정에서 임시로 붙였던 이름 일부를 사용자가 더 나은 표현으로
교정: Gluttony 폭식→탐식, Momentum 추진력→탄력, Nova 노바→신성,
Flexible 유연성→유연함, Inert 불활성→무기력. `carddefs.py` 안에 남아
있던 사용자 노출 문구("드러난 '노바' 카드를 선택하세요") 하나도
"신성"으로 맞춰 정리.

### 12.5 요약

| # | 카드 | 증상 | 근본 원인 | 수정 위치 |
|---|---|---|---|---|
| 1 | 색욕6(+불활성3) | 상대 손패 선택 화면 카드정보 null | `_serialize()`가 `e.turn`만 보고 손패 공개 여부 판단 | `web/app.py` |
| 2 | 압도2(+탄력0/압도1) | 3라인 중 1장만 채워지고 멈춤 | AI가 자기 라인부터 채워 FAQ룰대로 자멸 | `ai_prior.py` |
| 3 | 탄력1(+신성2) | 재배열 후 리액티브 전혀 발동 안 함 | `afterRearrange`를 어디서도 안 쐈음 | `engine.py` |
| 4 | 신성 표기 | "노바"로 잘못 표기된 문구 1곳 | 번역 확정 전 임시 이름 잔재 | `carddefs.py` |

전부 `pytest` 회귀 테스트로 고정했고(§11.5 기준 405개 → 최종
**407개**), 전체 스위트 통과 확인.

## 13. Main3/Aux3 90장 전수 검증 — 루아 대조 + reactive 배선 감사 + 대규모 자기대국

§12의 4건은 전부 사용자가 실제로 플레이하다 우연히 마주친 버그였다.
"운 좋게 걸린 것 말고 더 있지 않겠냐"는 사용자 지적에 따라, 이번엔
**우연이 아니라 체계적으로** 90장 전체를 훑었다.

### 13.1 방법 1 — 루아 원본 대 파이썬 포팅 라인별 대조

포팅 작업 때 참고했던 원본 루아 카드 정의(`carddefs_aux3.lua`,
`carddefs_main3_a/b/c.lua`)를 15개 프로토콜(Ambush~Rigid) 배치로 나눠
`TodoWrite`로 진행 상황을 추적하며 하나씩 대조했다. 대부분은 일치했고,
딱 한 종류의 불일치가 반복적으로 나왔다.

**`_move_one()`이 `can_move` 필터를 스스로 강제하지 않는다.** 같은
"카드 하나 골라서 처리" 계열 헬퍼인 `_flip_one()`은 내부적으로
`g.can_flip(x)`를 걸러주는데(`combined()` 래퍼), `_move_one()`은 그
보장이 없어서 **호출하는 쪽이 필터에 직접 `g.can_move(x)`를 넣어야만**
못 옮기는 카드(예: `Rigid_7` — "이 카드는 뒤집거나 이동시킬 수
없습니다")가 후보 목록에서 빠진다. 이 필터가 빠진 6곳을 찾아 전부
고쳤다:

```python
# 수정 전 (예: Nova_3)
lambda x, pi, l: _eff_val(x) < count

# 수정 후
lambda x, pi, l: _eff_val(x) < count and g.can_move(x)
```

대상: `Nova_2`(`_nova_2_after_rearrange`), `Nova_3`(`_nova_3_play`),
`Pride_0`(`_pride_0_play`의 두 분기 모두), `Flexible_1`
(`_flexible_1_play`의 이동 분기), `Flexible_3`(`_flexible_3_play`의
이동 분기).

다만 이건 **규칙 위반은 아니었다** — `Engine.move_card()` 자체에
`if not self.can_move(card): return False`라는 전역 가드가 이미 걸려
있어서(이전 세션에 추가), 못 옮기는 카드를 골라도 실제로는 조용히
아무 일도 안 일어나는 정도였다. 그래도 "선택 가능한데 눌러도 안
먹힘"은 UX 버그이므로 후보 목록 단계에서부터 걸러지도록 통일했다.
회귀 테스트 `test_move_one_candidate_lists_exclude_cantmove_cards`
추가(Pride_0 + Rigid_7 조합으로, 후보가 비어서 아무것도 안 움직임을
확인).

### 13.2 방법 2 — reactive 이벤트 전수 배선 감사

§12.3에서 `afterRearrange`가 통째로 안 쏘고 있었던 걸 발견한 뒤,
"또 이런 게 있지 않을까" 싶어서 90장이 쓰는 reactive/reactiveTop
이벤트 이름을 전부 뽑아서 `engine.py`에 실제로 `fire_reactive(...)`
호출이 있는지 하나씩 확인했다:

```bash
for ev in afterCache afterPlay afterRefresh afterDelete afterCompile afterRearrange afterGainControl; do
  echo "$ev: $(grep -c "\"$ev\"" src/game/engine.py)"
done
```

(처음에 `grep -n 'fire_reactive([^)]*"$ev"'`로 짰다가 `afterDelete`가
0건으로 나와서 "또 하나 빠졌다" 싶었는데, `self.actor()` 안에 괄호가
중첩돼 있어서 `[^)]*`가 중간에 끊긴 정규식 버그였다. 단순
문자열 카운트로 다시 확인하니 `afterDelete`는 3곳에서 정상적으로
쏘고 있었음 — 사용자에게 보고하기 전에 스스로 잡음.)

결과: 7개 이벤트 전부 최소 1곳 이상에서 발화 확인. `afterRearrange`
말고 추가로 빠진 건 없었다.

### 13.3 방법 3 — 대규모 자기대국 스트레스 테스트

`HeuristicAI` vs `HeuristicAI` 120판, 매 게임 신규 15개 프로토콜 중
최소 2~5개를 강제로 양쪽 덱에 섞어 넣어 신규 카드 노출 빈도를 높였다.
매 게임 종료 후 카드 총량 보존(양쪽 손패+덱+버림더미+3라인 스택 =
36장이어야 함, 프로토콜 6개×6장)을 불변식으로 체크:

```python
def card_count(e):
    total = 0
    for pi in (1, 2):
        p = e.players[pi]
        total += len(p['hand']) + len(p['deck']) + len(p['discard'])
        for l in (1, 2, 3):
            total += len(p['stacks'][l])
    return total
```

결과: **120판 전부 정상 종료(무한루프/미완료 0건), 카드 수 불일치
0건, 승률 59:61로 균형.** 카드가 증발하거나 복제되는 부류의 잠재
버그는 이 규모에서는 안 나왔다.

### 13.4 결론

`pytest -q` 전체 스위트 **407 → 408개 통과**(신규 회귀 테스트 1개
추가분). 이번 전수 검증에서 새로 발견된 건 `_move_one`/`can_move`
누락 1건뿐이고, 이마저도 전역 가드 덕에 실제 규칙 위반으로 이어진
적은 없었던 UX급 이슈였다. §12의 4건 + 이번 1건 = 총 5건을 잡은
뒤로는, 코드 로직(루아 대조) · 엔진 배선(reactive 감사) · 실행 시
안정성(대규모 자기대국) 세 방향 모두에서 추가 이상 신호가 없다.
